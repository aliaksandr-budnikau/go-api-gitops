apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: go-api
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: go-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s